[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bordle"
version = "1.0.0"
description = "Geography-based daily guessing game"
requires-python = ">=3.8"
